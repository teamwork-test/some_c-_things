#include <iostream>
using namespace std;

int main()
{
    int a, b;

    cout << "Input first number >> ";
    cin >> a;
    cout << "Input second number >> ";
    cin >> b;
    cout << "\nSum is >> " << a + b;
    return 0;
}
